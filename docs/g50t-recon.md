# g50t 探路（闭卷）

> 2026-09-10 · game_id=`g50t-5849a774` · tags=`["g50t_recon"]`

## 总览

| 关 | 状态 | 要点 |
|----|------|------|
| L1 | **PASS** (`levels` 0→1) | 四向 + 1 步缓冲；踩蛇头 + A5 闩锁；下井到目标 |
| L2 | **PASS** (`levels` 1→2) | 双收缩点；第二点 A5 闩锁 → 持久 e8=66；顶廊绕行到 ~(22,22) |
| L3 | **PASS** (`levels` 2→3) | tip1 闩76 → tip2 A5 → tip1 再离 → tip2 西离持久60 → 左廊进 (22,28) |
| L4 | **探路中** | tip(34,28) 持久36；左柱至 (10,40)；卡 y46→~(10,52) |

## READING 摘要

### L1
- `win_levels` = 7；`available_actions` = `[1,2,3,4,5]`；RESET `levels=0` `state=NOT_FINISHED`
- shape `64×64`；hist 约 `{0:3006, 1:9, 5:880, 8:82, 9:119}`
- **角色**：色9 空心 5×5 环（n=24），出生 ~(16,10)；只在色5 地板上走；步长 6
- **目标**：底带色9 团附近 ~(40,52)

### L2（对照 L1）
- **过渡**：L1 通关帧仍是 L1 终态；**下一拍任意动作**才加载真 L2（body≈1159）
- hist 约 `{0:2382, 1:18, 5:1479, 8:98, 9:119}`（无色2）
- 角色仍色9 n=24，出生 **~(52,28)**（右侧）
- **目标候选**：中部色9 团 n=19 ~(28.79, 22)；实测通关触发于 **~(22,22)**（`levels` 1→2）
- **四向 + 1 步缓冲**：迁移
- **蛇**：更大 n8≈98；**第一头** ~(40,28) 踩之 98→82；**第二收缩** ~(16,40) 再踩 82→66 且 `shaft_L=0`
- **A5**：第一头可闩；第二收缩点 A5（须缓冲技巧站稳）→ **死亡 + 闩锁**；之后再经 (28,40) 可得 **持久 e8=66**（离开不恢复）
- e8=82 时顶廊左行常堵；**e8=66 顶廊全开**

### L3（PASS）
- 过渡同 L2；出生 **~(10,22)**；**新色 11**；目标区 ~(22,22)，通关触发于 **(22,28)**（`levels` 2→3）
- **tip1 (40,34)**：A5 死；回访离开 → **持久 e8=76**
- **色11 穿闸**：顶→D×2→R 落 (34,22) 缩条；保持 shrink 时顶廊东→右柱可通底廊
- **tip2 (22,52)**：踩缩 76→60；**A5 武装**（表面 e8→92）；**勿再 A5 tip1**；仅 tip1 再离恢复 76 并武装 tip2 持久
- **持久 60**：武装后再次踩 tip2 再离开 → e8 保持 60（蛇 ymin≈37，**(22,34) 可走**）
- **清关**：持久60后西底→x10 上至 y34 → 东至 (22,34)→(22,28) → **`levels` 2→3**
- 复现：`tools/g50t_l3_seated_clear.py` · `tests/fixtures/g50t_l3_clear_attempt.json`（~208 acts）

### L4（进行中）
- 过渡同前：L3 通关下一拍加载；出生 **~(28,10)**；acts `[1..5]`；**新色 15**（n≈120，约 x∈[25,53] y∈[27,55]）
- hist 约 `{0:2513, 5:1274, 8:52, 9:119, 15:120, 1:18}`（无色11）；**玩家=色9**（~24px）
- **蛇**：单团 n=52，横条 y∈[26,30] x∈[8,35]；挡左柱与中列 D
- **tip1 (34,28)**：踩缩 **52→36**（hold-only）；A5 死；回访离开 → **持久 e8=36**（xmin≈14）
- **持久 36 后**：
  - 左柱 `(10,22)↓` 可穿至 **(10,40)**；**(10,40) 旁色9 非清关**（实为玩家）
  - 真目标候选：色9 n≈19 **~(10,52)**；y46 近全空（hold/persist 几何相同，不填桥）
  - 顶东绕行：`(34,22)→(40,22)→(40,10)→(52,10)→(52,28)` 可达色15 边缘；再 D/L 不行；A5@色15 死并丢持久
  - `(10,28)` 不能东踩蛇；`(28,22)` 不能 D 上蛇；tip 再 A5 丢持久
  - **色2 标记**：持久后 tip `(34,28)` 留色2（玩家离开时可见）；tour 中无第二标记
  - e8=52 可达覆盖仅 tip1 会缩
- 夹具：`g50t_l4_frame_live.json` · `g50t_l4_at1040_frame.json` · `g50t_l4_rightcol_live.json` · `g50t_l4_hold_tip_frame.json`

下一刀：找 tip2 / 色15 穿闸格（类 L3 @34,22）→ 底廊 ~(10,52) → `levels` 3→4

## 已坐实机制

1. **A1=UP / A2=DOWN / A3=LEFT / A4=RIGHT**，步长 6；撞墙/空洞/过窄走廊 → noop  
2. **1 步指令缓冲**：动作先入队，下一拍才执行；RESET 后第一拍常无位移  
3. **躲避/压制**：踩蛇头/收缩点 → 蛇收缩；**仅站定时保持**（未闩锁则离开恢复）  
4. **ACTION5 @ 收缩点**：闩锁；执行后常重生；闩锁语义关卡相关  
5. **L1 通关**：头上闩锁 → 左井下行 → ~(40,52) → `levels` 0→1  
6. **L2 通关**：第二收缩 A5 闩锁 → 持久 66 → 底廊东 → 右柱上顶 → 顶廊西 → 左柱下至 y=22 → 东进 → `levels` 1→2  
7. **L3 通关**：tip1 持久76 → 穿闸右柱底廊 → tip2 A5 武装 → tip1 再离 → tip2 再踩离开持久60 → 左柱上 → (22,34)→(22,28) → `levels` 2→3  

### L1 最小证据序列

缓冲感知序列（`tests/fixtures/g50t_l1_clear_attempt.json` · `head5_D_R`）：

`[4,4,4,4,4, 5, 2×…, 4×…]` → ~(40,52) 时 `levels` 0→1。

### L2 最小证据序列

`tests/fixtures/g50t_l2_clear_attempt.json` · `persist66_via_2840_top_loop`：

1. L1 清关 + 过渡拍进真 L2  
2. 至 (22,40) 发 A5（缓冲 LEFT 落入 (16,40)）→ 再一拍执行 A5（死+闩）  
3. 底路回到 (28,40) → e8 持久 66  
4. 下底廊东至 (52,52) → 上至 (52,10) → 顶廊西至 (10,10) → 下至 y=22 → 东至 ~(22,22) → **`levels` 1→2**

A5 队列技巧：在 (22,40) 发 A5，使已入队的 LEFT 先把人送进 (16,40)，下一拍 A5 才在收缩格执行。

### L3 最小证据序列

`tests/fixtures/g50t_l3_clear_attempt.json` · `persist60_west_clear`（`tools/g50t_l3_seated_clear.py`）：

1. L1+L2 清关进 L3  
2. tip1 (40,34) A5 → 回访离开 → 持久 e8=76  
3. 穿闸 → 顶廊东 → 右柱下底廊 → tip2 (22,52) A5（武装；表面 e8→92）  
4. **仅** tip1 再离（勿再 A5）→ e8 回 76  
5. 再至 tip2 → **西离** → **持久 e8=60**  
6. 底西至 x10 → 上至 (10,34) → 东 (22,34)→(22,28) → **`levels` 2→3**

注意：整段约 **≤210** 拍；过长会 `GAME_NOT_STARTED`（疑步数预算）。

## 单步差分注意

- 从 RESET **单发** A1–5 常 `full_ndiff=0`（易误判 noop）——须连发第 2 拍或先入队再冲刷  
- 忽略 y=63 bar 与顶栏生命色变做 body 计量  

## 卡点 / 下一刀

- **L4**：持久36 已稳；可达 (10,40)/(52,28)/tip；**y46 断层**；色15 边缘可站不可深入；c15 计数不随 tip 变
- 下一刀：类 L2 的 tip2（或色15 穿闸格）→ 底廊 ~(10,52) → `levels` 3→4
- 勿与 r11l / tr87 / vc33 共用 scorecard  

## 资产

| 类型 | 路径 |
|------|------|
| 探针 | `tools/g50t_recon_probe.py` · `tools/g50t_l2_recon_probe.py` · `tools/_g50t_l2_clear_v2.py` · `tools/g50t_l3_seated_clear.py` |
| 假设 | `docs/g50t-hypotheses.md` |
| L1 首帧 / 清关 | `tests/fixtures/g50t_l1_frame_live.json` · `g50t_l1_clear_attempt.json` |
| L2 首帧 / 清关 | `tests/fixtures/g50t_l2_frame_live.json` · `g50t_l2_clear_attempt.json` |
| L3 首帧 / 清关 | `tests/fixtures/g50t_l3_frame_live.json` · `g50t_l3_clear_attempt.json` |
| 索引 | `games/g50t/AGENTS.md` |

## 红线

不读引擎；tags=`["g50t_recon"]`；只认 `levels_completed`；**勿抢 r11l / tr87 / vc33 会话**。
