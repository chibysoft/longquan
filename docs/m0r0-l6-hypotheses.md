# m0r0 L6 坐实笔记

> 通关：水平合并后 **先 A1 离底垫，再 A3 压缩**。实现：`clear_l6`。

## 布局

| 元素 | 要点 |
|------|------|
| 出生 | `(22,22)/(38,22)`；`KinParams(4,4,60)` |
| 色 6\|7 | 墙 |
| 色 8 | 棋盘险区；足迹上再 ACTION → 软重置 |
| 色 9 | 一块 2×2，出生约 `(31,43)`；中缝分左右半区，不可跨半 |
| 色 12 | 顶左垫 `(18,14)` → 开右桥 BR12 `(38,30)–(49,33)` |
| 色 14 | 顶右/底左垫 → 开左桥 BR14；底垫参与终局合并 |

## 运动学（相对 L1–5）

- **Y 同向**：A1 双上、A2 双下（非 L1–5 的 Y 镜像）
- **X 镜像**：A3 外扩、A4 内收（同 L1–4）
- **幽灵按住**：踩垫时 A6 选标记，棋子变幽灵，桥仍开

## 通关路径（坐实）

1. 双踩顶垫 → 标记 `(19,19)` → 左按 c12，右经 BR12 到 y=42  
2. 标记 `(23,15)` 挡左 A4 → 右内收到 `(22,42)`  
3. 标记挪开 → **真 A4 换位** → `(22,14)/(18,42)`（右在底垫）  
4. 标记绕到 `(19,47)` → A2 → **水平合并** `(18,42,25,45)`  
5. **A1 离垫** → `(18,38,25,41)`  
6. **A3 压缩** → n10 32→16，`levels` 5→6（WIN）

## 证伪要点

- 合并后仍踩底垫时，A3/A4 **只拆开/再合并**，n10 不降。  
- x=30 险区几乎不可落；y=42 可穿。  
- 仅去色 8 的离线反相关 Y 模型有假阳性 mate，与 live 平行 Y 不符。

夹具：`tests/fixtures/m0r0_l6_enter.json`。  
复跑：`python tools/m0r0_seated_clear.py --max-levels 6`
