# ls20 L4 可证伪假设（武装 / 仪式）

> 2026-09-07 · 纯帧 + 线上 levels 判据 · 禁止罐头点击表

---

## 一句话

**L4 武装判据仍未知。** 结构差 + 双补给 + 软重置均已排除一批候选。

---

## 离线结构差（相对 L1–L3）

详见 `docs/ls20_l4_struct_diff_l1l3.md`。要点：

| L4 特异 | 含义 |
|---------|------|
| 无 plus | 不能搬 H23 |
| 门 `(1,1)` 与 spawn 同顶带 | 几何与 L3 底门不同 |
| warps=14 | 水平中轨多 |
| unlock `(6,6)` | **误报**（环不可达；`(7,5)` eject 入库后路径为 None） |
| 主色14 在图例 | 与 stamp 字形无匹配 |
| 离线不可达 | 底带 y=12、`(1,2)` 岛、环邻 `(6,5)/(8,5)` 等 15 格 |

---

## 本轮线上

| 实验 | 结果 |
|------|------|
| 双补给 / 软重置 | FAIL，gate 仍锁 |
| `(7,5)` eject + unlock 可达过滤 | L3 unlock 仍对；L4=`[]`；L1–L3 PASS |
| H5q / H5r 转移搜 | 无 gate_u；stamp_c9 6↔9 为 mover 叠 stamp 计量噪声 |
| H5s A5@(8,4) | 仅触发竖直门 → `(8,9)`，不开门 |
| H5t/H5u 覆盖 | 无 gate_u（燃油/早停） |
| H5w 模型失配 | **`(3,7)` 任意动作竖直门** → 已入库 |

---

## 仍排除的大类

环 unlock · plus/UDD · c0 · legend 翻转 · 双补给 · 软重置 · A5-on-stamp · 可达域短脉冲开门

---

## 下一刀

1. 继续 H5w：专扫贴不可达边界的站，找进 y=12 / `(1,2)` 的未建模门  
2. 或冻结主线为 **blocked-on-arming**（solver 已防假解锁）

探针：`h5w` · `h5r` · `struct_diff`
